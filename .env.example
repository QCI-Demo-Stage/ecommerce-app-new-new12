# PostgreSQL connection (local / ephemeral only — never commit real credentials)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ecommerce_dev

# Optional discrete settings (used if DATABASE_URL is unset)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=
PGDATABASE=ecommerce_dev
