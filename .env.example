# Payment service / migration env (local & ephemeral verification only).
# NEVER point DATABASE_URL at production, staging, or shared live databases
# from automated agents. Mutating migrate apply is withheld for humans.

DATABASE_URL=postgres://ecommerce:ecommerce_ephemeral@127.0.0.1:5432/ecommerce_verify
